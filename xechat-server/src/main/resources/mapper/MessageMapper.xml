<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xeblog.server.history.mapper.MessageMapper">

    <sql id="cols">
        id, created_at, channel, sender_account_id, sender_guest_uuid,
        sender_nickname, msg_type, content
    </sql>

    <insert id="insert" parameterType="cn.xeblog.server.history.entity.Message">
        INSERT INTO messages (
            id, created_at, channel, sender_account_id, sender_guest_uuid,
            sender_nickname, msg_type, content
        ) VALUES (
            #{id}, #{createdAt}, #{channel}, #{senderAccountId}, #{senderGuestUuid},
            #{senderNickname}, #{msgType}, #{content}
        )
    </insert>

    <select id="query" resultType="cn.xeblog.server.history.entity.Message">
        SELECT <include refid="cols"/>
        FROM messages
        WHERE channel = #{channel}
        <if test="sinceMs != null">
            AND created_at &gt; #{sinceMs}
        </if>
        <if test="beforeId != null">
            AND id &lt; #{beforeId}
        </if>
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

</mapper>
