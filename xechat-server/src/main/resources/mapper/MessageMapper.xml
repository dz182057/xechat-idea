<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xeblog.server.history.mapper.MessageMapper">

    <sql id="cols">
        id, created_at, sender_account_id, sender_guest_uuid,
        sender_nickname, msg_type, content, quote_json, recalled_at
    </sql>

    <insert id="insert" parameterType="cn.xeblog.server.history.entity.Message">
        INSERT INTO messages_public (
            id, created_at, sender_account_id, sender_guest_uuid,
            sender_nickname, msg_type, content, quote_json, recalled_at
        ) VALUES (
            #{id}, #{createdAt}, #{senderAccountId}, #{senderGuestUuid},
            #{senderNickname}, #{msgType}, #{content}, #{quoteJson}, #{recalledAt}
        )
    </insert>

    <select id="findById" resultType="cn.xeblog.server.history.entity.Message">
        SELECT <include refid="cols"/>
        FROM messages_public
        WHERE id = #{id}
    </select>

    <update id="markRecalled">
        UPDATE messages_public
        SET recalled_at = #{recalledAt}
        WHERE id = #{id} AND recalled_at IS NULL
    </update>

    <select id="query" resultType="cn.xeblog.server.history.entity.Message">
        SELECT <include refid="cols"/>
        FROM messages_public
        <where>
            <if test="sinceMs != null">
                AND created_at &gt; #{sinceMs}
            </if>
            <if test="beforeId != null">
                AND id &lt; #{beforeId}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

</mapper>
